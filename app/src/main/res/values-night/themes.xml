<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Glamping" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorSurface">@color/white</item>  <!-- Ensures white background -->
        <item name="android:windowBackground">@color/white</item>  <!-- Fixes full-screen background -->
    </style>
</resources>